<?php

/*
 * We need to print out a sheet with the inscription "KNOCK!"
 * and hang it on the door so that no one enters without knocking.
 * To do this, translate the word to uppercase using the strtoupper() function.
 * It accepts a string as input and returns the same one, but with all letters in uppercase.
 * Print the text written to the $text variable to the screen, converting it to uppercase.
 */

$text = 'knock!';
print_r(strtoupper($text));
